resource "aws_dynamodb_table" "lineage" {
  name         = "${var.project_name}-lineage"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "event_id"

  attribute {
    name = "event_id"
    type = "S"
  }
}
